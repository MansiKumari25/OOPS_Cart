<!-- <?php 
    
    // session_start();
    // require 'classes/product.php';
    // require 'classes/cart.php';

    // //$_SESSION["cart"]=new Cart();
    // $_SESSION["product"]=array();

    // foreach($_SESSION["products"] as $products)
    // {
    //     //print_r($products["id"]);
    //     $product=new Products($products["id"], $products["image"], $products["name"], $products["price"], $products["quantity"]);
    //     array_push($_SESSION["product"], $product);
    // }

    // echo "<pre>";
    // print_r($_SESSION["product"]);
    // echo "</pre>";

?> -->